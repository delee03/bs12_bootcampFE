import React from "react";
import module from "./demo.module.css";

const DemoCssModule = () => {
    return (
        <div>
            <h2 className={module.title}>Demo về CSS module</h2>
        </div>
    );
};

export default DemoCssModule;
